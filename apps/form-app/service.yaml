apiVersion: v1
kind: Service
metadata:
  name: form-app
  namespace: form-app
spec:
  selector:
    app: form-app
  ports:
    - name: http
      port: 80
      targetPort: 8000
      nodePort: 30474
  type: NodePort
